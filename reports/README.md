# Reports

This directory stores durable execution evidence.

- `reports/security/`: security validation outputs by run and stage
- `reports/slices/`: evidence bundles by backlog slice

Unlike `runtime/`, files here are intended to remain inspectable as durable acceptance evidence.
