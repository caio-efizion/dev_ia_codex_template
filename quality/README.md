# Quality Configuration

- `pipeline.config.template.json`: reusable template for blocking quality gates and frontend evidence
- `pipeline.config.json`: project-specific working configuration used by the pipeline when present
- `baselines/`: visual-regression baselines referenced by the evidence collector
