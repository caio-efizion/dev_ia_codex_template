# Security Layer

This directory holds the durable security model for the autonomous pipeline.

- `security-policy.md`: blocking security policy for the whole pipeline
- `agent-guards/`: per-agent input and output rules
- `validators/`: deterministic validators used before and after every stage
